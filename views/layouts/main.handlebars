<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Eat-Da-Burger!</title>
    <link rel="stylesheet" href="/assets/css/burger_style.css" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO"
        crossorigin="anonymous">
</head>

<body>

    {{{body}}}
    <!-- jQuery -->.
    <script src="https://code.jquery.com/jquery.js"></script>
    <script>
        function getBurgers(){
            $.get("/api/new",function(data){
                console.log("data------",data);
            })
        }
        $(".btn").on("click", function (event) {
            console.log("----------click---------");
            {{!-- event.preventDefault(); --}}
            var burgerNew = {
                burger_name: $('#burgerInput').val().trim()
            };
            
            // send an AJAX POST-request with jQuery
            /*
            var burgerItem = $("<input>");
            var submit = $("<input>");
            burgerItem.attr("placeholder", burgerNew.burger_name);
            submit.attr("type", "button");
            submit.attr("value", "Devour it!");
            $(".col-6 .text-left").append(burgerItem);
            $(".col-6 .text-left").append(submit);
            */
            {{!-- $.post("/api/new", burgerNew,getBurgers); --}}
            $.post("/api/new", burgerNew);

        });
        $(".devour").on("click", function (event) {
            {{!-- event.preventDefault(); --}}
             console.log("----------click---------");
            var burger = {
                id: this.id
            };
            console.log("id: ", burger.id);
            $.post("api/update", burger);
        });
    </script>
</body>

</html>